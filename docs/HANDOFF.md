## 2026-09-24 · PR #1011 publication

Published the isolated starter-authoring counterpart. Desktop PR #31 and all exact-head Builder CI are prerequisites; no automatic merge requested.

---

## 2026-09-24 · Phase 2.1 starter outcome windows

Read AGENTS.md and the TurnIntent rules. The fleet helper remains absent at both documented/local candidate paths; follow Miles's explicit isolated-worktree brief. Generic development-002 now authors a concrete private draft/clarification choice. All five realistic fixture families retain every existing task and gain one new development case with an actual inline fixture reference; manifests recompute counts. No hidden/trap case or promotion gate changes. Seven baseline failures become seven passes; the five relevant existing creation tests also pass after updating only the expected realistic-pack counts. Full CI and desktop PR #31 are prerequisites to merging. Scores before and after this change are not comparable as one trend.

---

# Session handoff

## 2026-09-24

Prepared Phase1.3 in fix/windows-codex-builder-env without modifying the running Builder checkout. Ported only the backed-up Windows environment allowlist. Five focused tests cover all twelve Windows variables, unchanged POSIX behavior, credential exclusion, empty values and CODEX_HOME. Before: one failed/four passed. After: five passed. Compilation and diff checks pass. Broader chip creation/bridge run: 44 passed/six failed; the same six tests fail with the original environment function. Three Windows path/python3 failures match; three provider-absence tests fail earlier on the original function (codex_cli_nonzero_exit) rather than at their fallback expectations. No claim of identical failure causes for those three. No full-suite or CI claim.

Desktop A2/A1 and authorized dependency/CI setup prerequisites have now landed after every required check passed. Publishing this Builder repair next in Phase1 order. GitHub access is resolved; do not request another login. The fleet helper named in AGENTS.md was absent at its documented path; isolated worktree follows Miles's explicit brief. No training data or running app changes. Published as PR #1010 with its changelog number. Merge only after all checks pass on the final revision.
